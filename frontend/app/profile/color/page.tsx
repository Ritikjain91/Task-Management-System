export default function ColorPage() {
  return (
    <div className="max-w-2xl px-4 py-6 sm:px-10 sm:py-10">
      <h1 className="mb-2 text-2xl font-semibold">Color</h1>
      <p className="text-sm text-[var(--color-ink-faint)]">
        Accent color customization isn&rsquo;t wired up yet — this page is a placeholder so the
        settings nav has somewhere to go.
      </p>
    </div>
  );
}
